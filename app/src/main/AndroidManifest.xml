<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.heigvd.pro.b04.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Polls"
        android:usesCleartextTraffic="@bool/config_allowClearTraffic">
        <activity android:name=".question.QuestionActivity" />
        <activity android:name=".poll.PollActivity" />

        <!-- Remote font loading from Google Fonts. -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/polls_fonts_preloaded" />

        <activity android:name=".home.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>